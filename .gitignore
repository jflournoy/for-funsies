# Node / TypeScript
node_modules/
dist/
build/
*.tsbuildinfo
.npm
npm-debug.log*

# Never commit these. CI installs with `npm ci --ignore-scripts`.
.env
.env.*
!.env.example

# Editor / OS
.DS_Store
.history/
*.swp

# Local agent/editor state — per-machine, not shared
.claude/
*.vexp-bak

# Resolved by the devcontainer CLI at build time; not pinned on purpose
.devcontainer/devcontainer-lock.json

# Python (tooling in scripts/ only)
__pycache__/
*.pyc
.venv/
venv/
